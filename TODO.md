# TODO - Bug inspection & fixes

- [ ] Fix safe guards in `src/lib/auth.ts` to prevent runtime crashes when `session.user` is undefined.
- [ ] Run `npm run lint`.
- [ ] Run `npm run build`.
- [ ] Review build/lint errors and apply fixes (API routes, auth/session, middleware, and upload/PDF flows).
- [ ] Re-run relevant test scripts (e.g. `test-*`).

